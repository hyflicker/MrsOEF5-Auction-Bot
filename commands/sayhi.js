exports.execute = (client, message, channel, tags, self, args, command) => {
    client.say(channel,`Everyone say hi to MrsOEF5 personal auction bot! LUL`)
    // console.log(tags.badges.hasOwnProperty('broadcaster'))
}